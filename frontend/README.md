# HabitusX frontend

Not started. The UI phase begins after the public API exists (roadmap PR 5) and will be
built to a design reference supplied by the owner.

Planned stack: Next.js with TypeScript, consuming the read-only API from `backend/`.
